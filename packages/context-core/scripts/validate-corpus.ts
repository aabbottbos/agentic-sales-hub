// packages/context-core/scripts/validate-corpus.ts — walks context/, validates every file
// against its schema, verifies inbound source_hash, cross-checks crm_id/account_slug == dir.
// Implemented in build step 3.11 (plan 001 §2).
export {};
