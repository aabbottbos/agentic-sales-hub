---
name: find-evidence
description: Find and rank the org evidence, ICP material, and prior deal artifacts that match a described deal situation. Returns a ranked list of cited spans; no synthesis, no prose.
allowed-tools: context.read, context.search
---

# find-evidence

> **Generated from `packages/skills/src/definitions/find-evidence.yaml`.**
> Do not edit this file by hand. Run `pnpm skills:sync` after changing the
> definition; CI fails on drift.

**Tier:** retrieval · **Version:** 1

Find and rank the org evidence, ICP material, and prior deal artifacts that match a described deal situation. Returns a ranked list of cited spans; no synthesis, no prose.

## How to run it

Invoke `runSkill('find-evidence', input, ctx)` from `@deal-desk/skills`. Do not
re-implement the logic inline — the one definition drives this skill, the MCP
tool, and the eval runner.

## Inputs

- `situation` (string, required)
- `account_slug` (string, optional)
- `k` (number, optional)

## Context read scope

  - `context/org/**`
  - `context/demand-gen/**`
  - `context/accounts/*/opportunities/*/artifacts/**`

## Output contract

`context/schema/retrieval-result.json` — every material claim carries a resolvable citation.

## Evaluated by

`evals/cases/find-evidence/`
