---
name: call-summary
description: Summarize one sales meeting note into a structured call summary — a prose recap plus extracted commitments, next steps, and opportunity context deltas, each carrying a resolvable citation into the note. Claims with no source are marked [unsourced], never asserted. Returns a structured object; a separate step (WI-2) persists it.
allowed-tools: context.read
---

# call-summary

> **Generated from `packages/skills/src/definitions/call-summary.yaml`.**
> Do not edit this file by hand. Run `pnpm skills:sync` after changing the
> definition; CI fails on drift.

**Tier:** generation · **Version:** 1

Summarize one sales meeting note into a structured call summary — a prose recap plus extracted commitments, next steps, and opportunity context deltas, each carrying a resolvable citation into the note. Claims with no source are marked [unsourced], never asserted. Returns a structured object; a separate step (WI-2) persists it.

## How to run it

Invoke `runSkill('call-summary', input, ctx)` from `@agentic-sales-hub/skills`. Do not
re-implement the logic inline — the one definition drives this skill, the MCP
tool, and the eval runner.

## Inputs

- `meeting_path` (string, required)

## Context read scope

  - `context/accounts/*/opportunities/*/meetings/**`

## Output contract

`context/schema/summary-output.json` — every material claim carries a resolvable citation.

## Evaluated by

`evals/cases/call-summary/`
