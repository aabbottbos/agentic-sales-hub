---
name: sow-review
description: Review a counterparty SOW or MSA redline against Meridian's legal guidance and clause library. Returns structured findings only — never prose. Each finding cites the clause-library entry that establishes the position it violates. Has no write grant; a separate step persists findings.
allowed-tools: context.read
---

# sow-review

> **Generated from `packages/skills/src/definitions/sow-review.yaml`.**
> Do not edit this file by hand. Run `pnpm skills:sync` after changing the
> definition; CI fails on drift.

**Tier:** review · **Version:** 1

Review a counterparty SOW or MSA redline against Meridian's legal guidance and clause library. Returns structured findings only — never prose. Each finding cites the clause-library entry that establishes the position it violates. Has no write grant; a separate step persists findings.

## How to run it

Invoke `runSkill('sow-review', input, ctx)` from `@deal-desk/skills`. Do not
re-implement the logic inline — the one definition drives this skill, the MCP
tool, and the eval runner.

## Inputs

- `document_path` (string, required)

## Context read scope

  - `context/legal/guidance.md`
  - `context/legal/clause-library/**`
  - `context/accounts/*/opportunities/*/inbound/**`

_No write grant. Review skills return findings; a separate step persists them._

## Output contract

`context/schema/finding.json` — every material claim carries a resolvable citation.

## Evaluated by

`evals/cases/sow-review/`
