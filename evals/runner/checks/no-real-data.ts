// evals/runner/checks/no-real-data.ts — CI guard: every context/accounts slug is in the
// synthetic namespace and every context file carries `fictional: true`.
// Implemented in build step 8 (plan 001 §8).
export {};
